import { Link } from "solid-app-router";

export default function NotFound() {
  return (
    <main>
      <div id="l">
        <h1>Page Not Found</h1>
        <p>you could always <a href="/">go home</a></p>
      </div>
    </main>
  );
}
